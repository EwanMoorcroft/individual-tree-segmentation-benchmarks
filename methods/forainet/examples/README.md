# Public-safe examples

`checkpoint_exposure_32_plots.csv` is a metadata-only exposure table. It
contains no point coordinates, raw data, predictions, credentials or private
paths. `exposure_evidence_sources.json` records the immutable upstream commit,
source-file blob identities, persistent URLs and checkpoint hash supporting
that table. Runtime examples will be added only after retained Barkla evidence
has passed the corresponding gate.
