# ForestFormer3D shared integration manifest

Status: prepared structure only; do not apply.

After both authorised held-out variants complete and this branch is selected
for integration, this document will enumerate exact proposed changes to:

- repository method indexes;
- `CITATION.cff`;
- the benchmark result registry;
- the test-exposure ledger;
- the method-development budget;
- environment provenance;
- prediction retention;
- diagnostic availability;
- canonical plot, site and overall result tables; and
- deterministic governance and workbook rebuild commands.

No candidate or pending shared registry row is requested before retained
held-out evidence exists.
